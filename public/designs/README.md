# Designs

Drop your design images here (PNG / JPG / WebP), then reference each from
`data/designs.ts`, e.g. `image: "/designs/my-design.png"`.

Cards keep each image's natural aspect ratio, so any dimensions work. Until a
real file exists at the referenced path, the gallery shows a clean placeholder.
